<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
    </style>
</head>
<body>
<div style="width: 85%; margin: 0 7.5%;">
    <div style="text-align: center;">
        <img src="<%= bookCover %>" />
        <table>
            <% if (students.length > 0) { %>
                <% students.forEach(student => {%>
                    <tr><td><%= student.type %></td><td><%= student.position %></td><td><%= student.text %></td></tr>
                <%  });%>
            <% }%>
        </table>
    </div>
</div>
</body>
</html>
